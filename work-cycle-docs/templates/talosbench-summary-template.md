# TalosBench Summary Template

```text
PASS:
FAIL:
PARTIAL:
UNSUPPORTED:
POLICY_DENIED:
AUDIT_DESIGN:
NOT_RUN:
```

Use `AUDIT_DESIGN` only when the test setup or evidence capture is invalid, not when Talos itself failed.

